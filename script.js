function timesTwo(number) {
  var product = number * 2;
  return product;
}

function minusThree(number) {
  var difference = number - 3;
  return difference;
}

function addTen(number) {
  var sum = number + 10;
  return sum;
}

addTen(minusThree(timesTwo(2)));